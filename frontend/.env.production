VITE_APP_NAME=ConsultPro
VITE_APP_VERSION=1.0.0
# Cloudflare Turnstile site key for /signup (Workstream T9). Leave empty to
# skip client-side widget rendering — backend also skips verification when
# TURNSTILE_SECRET_KEY is unset, so signup stays usable either way.
VITE_TURNSTILE_SITE_KEY=
